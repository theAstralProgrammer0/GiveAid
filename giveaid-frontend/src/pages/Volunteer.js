import React from 'react';

const Volunteer = () => {
  return (
    <section id="volunteer" className="volunteer-container bg-light container">
      <h2 className="section-title">Be a Volunteer</h2>
      <p className="text-large">Join us in making a difference by volunteering for a cause.</p>
      {/* Add volunteer form and content as per design */}
    </section>
  );
};

export default Volunteer;

